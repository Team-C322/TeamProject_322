package allclasses.karachebanDI;

/// Стандартный ненужный интерфейс но для разнообразия можно
public interface Furniture {
    String getType();
    long getSerialNumber();
    void ShowPrice(String currency);
}
